@*@model AspNetCore.Models.Customer*@
@*@{
    int age = 25;
    //string value = string.Empty;
    //if(age>18)
    //{
    //    value = "Yaşı 18 den büyük";
    //}
    //else
    //{
    //    value = "Yaşo 18 den küçük";
    //}
    int Calculate(int number1, int number2) //C# da method isimleri büyük harfle başlar
    {
        return number1 + number2;
    }
    var result = Calculate(1, 2);

    List<string> names = new() { "Songül", "Sinem", "Aslı" };
}
@foreach (var item in names) // Tab Tab
{
    <p>
        @item
        @(item=="Songül"||item=="Aslı"?"yazılımcı":"İnsan Kaynakları")
    </p>
}
<p>
    Durum = @(age>18?"Yaşı 18 den büyük":"Yaşı 18 den küçük") <br />
    Durum2 = @result;
</p>
<p>
   ViewBag.Name=@ViewBag.Name
</p>
<hr />
<p>
    ViewData["Name"]=@ViewData["Name"]
</p>
<hr />
<p>
    TempData["Name"]=@TempData["Name"]
</p>
<hr />
<p>
    First Name = @Model.FirstName;
    <br />
    Last Name = @Model.LastName;
    <br />
    Age = @Model.Age;
</p>*@
@model List<AspNetCore.Models.Customer>
<html xlmns="http://www.w3.org/1999/xhtml">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
<head>
    <title>Customer-List</title>
    <link href="~/style.css" rel="stylesheet" />
</head>
<body>
    <div class="container">
        <h3>Customer List</h3>
        <a href="/Home/Create">Ekle</a>
        <table class="table table-hover table-bordered">
            <thead>
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Age</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.FirstName</td>
                        <td>@item.LastName</td>
                        <td>@item.Age</td>
                        <td>
                            <a href="/Home/Update/@item.Id">Update</a>
                            <a href="/Home/Remove/@item.Id">Remove</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>


